{
  // start
  // Problem 1:
  const sumArray = (arr: number[]): number => {
    let result: number = 0;
    for (let i: number = 0; i < arr.length; i++) {
      result += arr[i];
    }
    return result;
  };

  // end
}
